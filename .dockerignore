# Keep the build context (this repo) small and free of secrets.
target/
.git/
.github/

# Secrets must never enter the image.
.env
.env.*
*.jsonl
deploy/secrets/
deploy/.env

# Not needed to build the server.
docs/
*.md
