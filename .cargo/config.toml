# Fetch git dependencies with the system git client, so they honour your git
# configuration (proxies, URL rewrites, credential helpers) instead of libgit2's
# limited handling. The Dockerfile sets CARGO_NET_GIT_FETCH_WITH_CLI.
[net]
git-fetch-with-cli = true
